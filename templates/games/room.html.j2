<!DOCTYPE html>
<html>
    <head>
        <title>{{ g.name }} - Room {{ room_id | format_id(4) | safe }}</title>
        <meta charset="utf-8">
        <link rel="stylesheet" type="text/css" href="{{ url_for('static', filename='games/room/room.css') }}">
        <script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
    </head>
    <body>
        <div class="main">
            <div class="player-list">
                <ul>
                    {% for u in users %}
                        <li id="{{ u.user_id }}">
                            <span> {{ u.username }} </span>
                        </li>
                    {% endfor %}
                </ul>
            </div>
            <div class="room-info">
                <span class="game-name golden-text">{{ g.name }}</span>
                <button class="copy">
                    <svg class="icon">
                        <use href="#copy-icon"></use>
                    </svg>
                    <span>Copy Invitation</span>
                </button>
                <button class="leave">
                    <svg class="icon">
                        <use href="#goback-icon"></use>
                    </svg>
                    <span>Leave Room</span>
                </button>
                <button class="start">
                    <svg class="icon">
                        <use href="#start-icon"></use>
                    </svg>
                    <span>Start Game</span>
                </button>
            </div>
        </div>
    </body>

    <script>
        window.roomID = {{ room_id }};
        window.gameID = {{ g.id }};
        window.gameName = "{{ g.name }}";
    </script>
    <script type="module" src="{{ url_for('static', filename='games/room/room.js') }}"></script>
    {% include "/home/svgs.html.j2"%}
</html>